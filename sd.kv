MDBoxLayout:
    orientation: 'vertical'
    MDScreen:
        MDLabel:
            id:my_label
            text: "Some stuff"
            halign: 'center'
        
        MDFloatingActionButtonSpeedDial:
            icon: "pencil"
            data: app.data
            root_button_anim: True
            # root_button_anim: False
            #label_text_color:1,0,0,1#text color
            # bg_color_stack_button:(1,0,0,1)#bg of icon
            # bg_color_root_button:(1,0,0,1)#bg of PLUS ICON OF SPEED BUTTON DIAL   
            # color_icon_root_button:(1,0,0,1)#bg of spped dial text 
            # color_icon_stack_button:(1,0,0,1)#icon text color not background color
            # bg_hint_color:(1,0,0,1)# hint color 
            on_open:app.open()
            on_close:app.close()